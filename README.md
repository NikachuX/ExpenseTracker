# 📈 УЧЕБНЫЙ ПРОЕКТ: Expense Tracker (Трекер расходов)

Данный репозиторий содержит исходный код микросервисного веб-приложения "Expense Tracker", разработанного в рамках курсовой работы по теме **«Настройка и администрирование управления тестовой средой для разработки
и тестирования веб-приложений»**.



---

## 🏗️ Архитектура системы (Микросервисы)

Приложение реализовано на основе микросервисной архитектуры и состоит из четырех независимых сервисов и одной общей базы данных.

| Сервис | Порт (Локально) | Основная функция | Технологии |
| :--- | :--- | :--- | :--- |
| **`auth-service`** | 5000 | Регистрация, аутентификация, генерация JWT-токенов. | Flask, SQLAlchemy, Psycopg2 |
| **`transactions-service`** | 5001 | Добавление, хранение и чтение финансовых операций. | Flask, SQLAlchemy, Psycopg2 |
| **`reports-service`** | 5002 | Агрегация данных и формирование отчетов (аналитика). | Flask, SQLAlchemy, Psycopg2 |
| **`ui-service`** | 5003 | Шлюз (BFF - Backend for Frontend), обработка сессий, редиректы, обращение к бэкенд-сервисам. | Flask, Requests |
| **`db`** | 5432 | Единое хранилище данных. | PostgreSQL 15 |

## 🛠️ Основной Технологический Стек

* **Разработка:** Python 3.10, Flask
* **База данных:** PostgreSQL
* **Контейнеризация:** Docker
* **Оркестрация (Production):** Docker Swarm
* **Автоматизация (CI/CD):** GitHub Actions
* **Тестирование:** Pytest (Unit Tests, E2E Tests)

---

## 🚀 Локальный Запуск (Среда Разработки)

Для запуска всех сервисов и базы данных в локальной среде (Development/Test) используйте Docker Compose.

### 📋 Требования

1.  Установленный Docker и Docker Compose.
2.  Наличие файла `.env` с переменными окружения (DB credentials, JWT keys).

### ⚙️ Инструкция по запуску

1.  Склонируйте репозиторий:
    ```bash
    git clone [URL_РЕПОЗИТОРИЯ]
    cd expense-tracker
    ```

2.  Соберите и запустите все контейнеры:
    ```bash
    # Для простого локального запуска
    docker compose up --build -d
    ```

3.  Приложение будет доступно по адресу: `http://localhost:5003`

---

